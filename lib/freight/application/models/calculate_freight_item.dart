class CalculateFreightItem {
  final num width;
  final num height;
  final num length;
  final num weight;
  final num quantity;

  CalculateFreightItem(
      this.width, this.height, this.length, this.weight, this.quantity);
}
